import { View, Text  ,SafeAreaView , ScrollView} from 'react-native'
import React from 'react'

const Home = () => {
  return (
    <SafeAreaView>
       
    <Text>welcome to the new booking app  aw</Text> 
        <ScrollView>
            <Text> this the first page of the booking app </Text>
        </ScrollView>
</SafeAreaView>
  ) ;
}

export default Home